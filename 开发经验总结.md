# 招标文件快速解压工具 — 开发经验总结（v2.4 → v3.0）

> 本文档记录 v2.4 到 v3.0 迭代中踩过的坑、打通的链路与可复用的方法。
> 项目：`extract_tool.py`（单文件 tkinter 应用，PyInstaller onefile 分发）

---

## 0. 各版本一句话回顾

| 版本 | 主题 | 核心动作 |
|---|---|---|
| v2.5 | Motrix 深度链接 | 逆向 `motrix://` 协议，附件下载从浏览器改为 Motrix |
| v2.6 | 无头下载 | 发现本地 aria2 RPC 配置持久化，直控引擎；行内进度条 + 暂停/停止/重下 |
| v2.7 | 直链解析 | 诊断"4KB 假下载"，epoint 查看页自动还原真实文件直链 |
| v2.8 | 视觉重构 | 复刻 Hermes Teal 暗色终端风；内置免费字体 MiSans |
| v3.0 | 安庆清单解密 | 逆向 .AQZF 内层 .18aqzb 加密（MD5(EncryKey) 轮换 → AES-128-ECB），解压产物与官方"导出招标解密清单"逐字节一致 |

---

## 0.1 v3.0 案例：.AQZF 内层清单解密全链路（逆向方法论模板）

**症状**：工具解压安庆 `.AQZF` 正常出 PDF/RAR，但 `.18aqzb` 用官方软件打开报错；
官方工具导出的 18aqzb 与工具产物差 14 字节（909472 → 909458）。

**定位链**（复用价值极高，其它平台同构问题照抄即可）：

1. **先做字节级对比**：密文长度 = 明文按 PKCS7 补齐到 16 字节边界 → 判定 AES+PKCS7；
   `AQZF` 本体是 `<ZBFile>` XML 包 base64(ZIP)，尾部 `EncryMode=3`、`GMtype=EPGM_0`。
2. **找官方实现位置**：全盘扫 `AQZF/18aqzb` 字符串 → 命中
   `新点投标文件制作软件(安徽省版)\安徽省\EpointTBTool.UI.DQAnHui.dll`（**明文串**）。
3. **官方会留解密现场**：`安装目录\FileTemp\解密<原名>.18aqzb` —— 用户曾用官方工具
   导出过，现场直接拿到"标准答案"（SHA-256 比对用）。
4. **关键密钥不在代码里，在容器里**：`PBZB.xml`（容器内部件，工具一直跳过写盘）
   的 `<ZBInfoMx BookMarkName="EncryKey" MarkValue="<GUID>"/>` + `EncryQingDan="1"`。
5. **原生 oracle 验证算法**：32 位 PS `LoadLibrary(EPointCA.dll)` →
   `GetProcAddress("EpDecryptFile2024")` → 自造密文（key=md5("test")）喂进去能还原 →
   证实 `AES-128-ECB/PKCS7、key=MD5(strID)`。反汇编 0x1000ccc0 可见 0x80(=128bit)
   密钥扩展 + 16 字节块循环 + 尾字节去填充。
6. **最后一个坑：密钥轮换**。原生函数里 MD5 摘要使用前与内置全局比对，
   命中则做「前8/后8字节轮换」。实测**轮换才是真实使用的形态**：
   `key = MD5(EncryKey)[8:16] + MD5(EncryKey)[0:8]`，不轮换解出来是乱码。

**通用教训**：
- `.NET 侧`：`LoadFrom` 后找 `(Int32)->String` 静态方法即可批量还原 ConfuserEx 字符串；
  `ReflectionOnlyLoadFrom` + `ResolveString` 处理不混淆的程序集更快。
- `python -c` 一行式在 PowerShell 里传中文必炸，测试脚本一律写文件；
  ps1 含中文路径时**写盘为 UTF-8 无 BOM 会被 PS5.1 当 ANSI 读**——用纯 ASCII
  逻辑（如 `Get-ChildItem | Where TestPath(EPointCA.dll)`）发现路径，别写中文字面量。
- 32 位原生 DLL 要用 `C:\Windows\SysWOW64\WindowsPowerShell\v1.0\powershell.exe`
  + `SetDllDirectory` + 全路径 `LoadLibrary` 三件套；`GetProcAddress` 为 0 时
  首先怀疑加载到了 PATH 上另一个同名 DLL。
- 猜密钥派生（md5/sha1/sha256/sm3 × 各种编码）之前，先反汇编看一眼常量指纹
  （MD5 init `67452301...`、AES S-box `637c777b...`），再拿 oracle 验证假设。

**后续可扩展**：`.ahaqslzb`（decrypt_config 里的旧占位规则）及其它新点系容器
若同样"官方有解密、密钥在 PBZB.xml"，可直接套用 `algorithm: "AQZF-PBZB"` 规则，
无需写码。

---

## 1. Motrix 集成：三级跳

### 1.1 第一级：深度链接（弃用，但方法值得记）

**逆向路径**：Electron 应用的协议处理逻辑在
`resources/app.asar`（主进程，压缩 JS）和 `resources/app.asar.unpacked/dist/renderer/`（渲染进程，**明文**）。

- 二进制 asar 里找字符串：PowerShell 按 byte 扫描定位 `motrix://` 上下文；
- 渲染端逻辑去 `app.asar.unpacked` 明文 JS 里正则搜索（快得多）。

**v2.0-beta.33 的协议格式**（与网上流传的 1.x base64-JSON 格式完全不同）：

```
motrix://new-task?uri=<percent-encoded 下载链接>
```

- 解析链：`new URL(n)` → `hostname === 'new-task'` → `searchParams.get('uri')`
  → `fW(uri)`（要求以 http/https/ftp/magnet 开头）→ 打开「新建任务」窗口预填；
- 新建任务文本框按**换行**拆分多链接（`urls.split('\n')`），故一次调用可批量预填；
- `URLSearchParams` 解码时 `+` 会变空格——`quote(payload, safe="")` 全量编码即可规避。

局限：只能"预填待确认"，无法免弹窗、无法指定保存目录 → 引出第二级。

### 1.2 第二级：本地 RPC 无头直控（v2.6 核心）

**关键发现**：Motrix 把引擎 RPC 端口与令牌**持久化**在
`%APPDATA%\Motrix\settings.json`：

```json
"engine": { "rpcPort": 16800, "rpcSecret": "YJAHY7vF", ... }
```

`aria2-owner.json`（同目录）记录进程归属与启动参数，可交叉验证。
由此即可用标准 aria2 JSON-RPC（`http://127.0.0.1:<port>/jsonrpc`，首参 `token:<secret>`）
实现 `addUri / tellStatus / pause / unpause / remove`，全程无窗口、任务自动开始，
且任务会出现在 Motrix 主界面统一管理。

`addUri` 选项实战：

```python
opts = {"dir": 项目输出目录, "out": 清洗后的文件名,
        "user-agent": UA, "referer": 原页面,
        "allow-overwrite": "true",           # 「重新下载」可覆盖
        "continue": "true" if resume else "false"}
```

**Motrix 定制 aria2 的三个坑**（vanilla aria2 没有的行为）：

1. **完成任务立刻从 RPC 消失**（SQLite 持久化模式）。对已消失的 GID 查询返回
   **HTTP 400**，错误详情在响应体 JSON 里（`GID xxx is not found`）。
   → RPC 封装要捕获 `HTTPError` 解析响应体；轮询器对 "not found" 走
   **磁盘文件判定**：文件大小 ≥ 最后已知总量 → 完成；有文件但偏小 → 中断；
   无文件 → 任务丢失。
2. **`continue=true` 对不支持 Range 的服务器会直接报错**
   （如 Cloudflare 测速端点返回 200 全量 → `Invalid range header`）。
   → 「重新下载」一律 `continue=false` 强制全新下载；首次提交才尝试续传。
3. GID 消失也可能是**引擎重启**——错误文案要区分引导（"请重新下载"）。

### 1.3 状态机与线程模型

行状态机：`idle → starting → active → paused → complete / error / stopped`。

- **只有 UI 线程碰 tkinter 控件**；后台线程（提交动作、1 秒轮询）一律
  经 `queue.Queue` 投递 → UI 侧 `root.after(150, ...)` 消费；
- 操作类 RPC（暂停/停止）失败**不硬切状态**，交给下一秒轮询用引擎真实状态纠正；
- 鼠标滚轮用 `bind_all` + **指针落点矩形判断**，避免劫持其它控件的原生滚动。

---

## 2. "4KB 假下载"诊断：一条完整的取证链

用户实测：Motrix 显示下载"成功"，文件却只有 3677 字节、打开损坏。

**取证路径**（按性价比排序）：

1. **查 Motrix 任务库** `%APPDATA%\Motrix\motrix.db`（SQLite，表
   `tasks` / `task_instances.uris`）→ 拿到用户实际下载的 URL 与 save_dir；
2. **打开那个 3677 字节的文件**——不是登录页，是 epoint 平台的
   **图纸查看器页面**（`TuZhiDocShow?AttachGuid=...`）；
3. **读页面 JS**：`SaveFile()` 按钮的实现揭示真实端点——
   `epoint.initPage('TuZhiDocShowAction')` 返回 JSON，其中
   `custom.serverFilePath` 才是真链；
4. **直接探测 Action 端点**（页面路径 + `Action.action` + 原查询串）：
   实测**无需登录态**（AttachGuid/ClientGuid 本身就是访问凭证），
   返回的 `TuZhiDownloadAttachment.action?cmd=download&...` 直连就是 RAR 魔数。

**结论**：平台给的是"页面"而非"文件"。v2.7 在提交 Motrix 前加解析层：

```
GET 原链接（只读 2KB 头）→ 命中 HTML？
  → 拼 Action.action 接口 → JSON 取 custom.serverFilePath → 用它提交
  → msg 非空（如"链接已过期"）→ 抛错把平台原话显示到行内
```

配套兜底：下载完成后**嗅探文件头**（`<!DOCTYPE html` / `<html`，容忍 UTF-8 BOM），
仍是网页 → 行内报错引导「浏览器」打开，杜绝"看似成功的坏文件"。

> 教训：**"下载成功"≠"下载对了"**。对未知平台，先看产物字节，再谈鉴权方案。
> 我们一开始准备做的"浏览器 Cookie 借取"（DPAPI/AES-GCM 解密 Chrome Cookie）
> 方向就错了——正确诊断让方案大幅简化。

---

## 3. ctypes 调 DPAPI：堆损坏惨案

诊断 Cookie 时写的 `CryptUnprotectData` 调用**没设 argtypes**，
64 位下指针被按 int 截断 → 进程直接堆损坏（exit `0xC0000374`，无任何输出）。

**正确写法**（务必声明原型）：

```python
class BLOB(ctypes.Structure):
    _fields_ = [("cbData", wintypes.DWORD),
                ("pbData", ctypes.POINTER(ctypes.c_ubyte))]

_CryptUnprotectData = ctypes.windll.crypt32.CryptUnprotectData
_CryptUnprotectData.argtypes = [ctypes.POINTER(BLOB), ctypes.POINTER(ctypes.c_wchar_p),
                                ctypes.POINTER(BLOB), ctypes.POINTER(BLOB),
                                ctypes.c_void_p, wintypes.DWORD, ctypes.POINTER(BLOB)]
_CryptUnprotectData.restype = ctypes.c_bool
```

排查技巧：**静默崩溃 + 无输出 = 先怀疑 ctypes 原型**，而不是逻辑 bug。
（顺带：本机最终发现平台登录态并不可 stealing——多为会话 Cookie 或魔改加密，
如 360se 的 v10 密钥非标准 DPAPI。Cookie 方案对"盗版式集成"要降低预期。）

---

## 4. 免费字体落地（替代微软雅黑）

诉求：界面全量换免费可分发字体（微软雅黑有授权顾虑）。

**选型结论**：**MiSans**（小米官方，允许免费商用与再分发），
HarmonyOS 同风格的中性黑体；等宽小件（版本号/计数/百分比）用系统 **Consolas**
（仅 ASCII 场景，无分发问题）。

**落地三步**：

1. 字体文件放 `fonts/`，PyInstaller spec 加
   `datas=[('<项目>/fonts', 'fonts')]` → 运行时解包到 `sys._MEIPASS/fonts`；
2. 启动时私有注册（不污染系统，卸载即走）：

```python
ctypes.windll.gdi32.AddFontResourceExW(path, FR_PRIVATE /*0x10*/, 0)
```

3. 注册成功 → `FONT = "MiSans"`；失败回退系统字体（别人的机器没带字体也不崩）。

**坑**：
- `AddFontResourceExW` 返回值 >0 即成功（可能注册多个 face）；
- Tk 的字体族名取字体内部名（"MiSans"），`tkinter.font.families()` 可验证；
- **全局 FONT 在 `make_root()` 里才被覆写**——import 后立刻读全局值会拿到
  回退字体（测试断言要放在构造窗口之后）；
- exe 从 12MB → 22MB（两个字重 ~15MB），单文件分发可接受。

---

## 5. tkinter 深色主题（复刻 Hermes Teal）

**设计基因提取**：直接读目标应用的构建产物
（`web_dist/assets/index-*.css`），抓三样东西：

1. `:root` 里的 CSS 变量（背景 #041c1c / 强调 #ffac02 / 文字 #ffe6cb）；
2. 语义色（success #4ade80 / warning #ffbd38 / destructive #fb2c36）；
3. 透明度变体规则（text-secondary = 强调色 65% 混底色 → 预计算成 #a79f8d）。

**tkinter/clam 实现要点**：

- `ttk.Style(theme_use("clam"))` 才能完全自定义配色；
- 按钮 = 描边式（`borderwidth=1, relief="solid", bordercolor=...`），
  hover 用 `style.map` 换 `bordercolor`/`foreground`（比换底色更贴终端风）；
- 主按钮强对比：琥珀底 + 近黑字（`#1c1000`），disabled 显式给灰阶；
- tk 原生控件（Listbox/Text/Canvas）要逐个设 `bg/fg/insertbackground/
  selectbackground`，漏一个就突兀；
- 发丝线用 `tk.Frame(height=1, bg=BORDER)`（比 TSeparator 可控）；
- 布局仿目标应用：左侧品牌栏（品牌 + 实时统计 + 主题徽标）+ 顶部标题行 +
  底部状态栏，信息层级立刻"像了"。

**验证闭环**：写一个预览脚本——注入模拟数据（各种行状态）→ `root.update()`
循环驱动 → `ctypes.FindWindowW + GetWindowRect` → `PIL.ImageGrab` 截窗口 →
人眼比对。**截图比想象可靠**：本次靠截图发现右侧面板被裁切、行按钮溢出，
窗口加宽 + 新增紧凑按钮样式（`Row.TButton`）后一次到位。

---

## 6. 工程化杂项

- **纯函数拆分换可测性**：解析层拆成 `_looks_like_html` / `_epoint_action_url`
  / `_parse_epoint_server_file_path` 三个纯函数，网络部分只剩薄薄一层，
  48 个单测里网络零依赖；
- **沙箱环境后台进程会被回收**：本地 HTTP 服务器起在独立进程会被杀——
  改为**单进程内起线程**（`http.server` + daemon thread）一气呵成；
- **PowerShell 5.1 不支持 `??` 运算符**、`--jq` 带空格/方括号的表达式会被
  拆参——复杂 jq 写进变量或改用简单输出过滤；
- **GitHub 推送**：`gh` 已登录但 git 不在 PATH——GitHub Desktop 自带
  `app-*/resources/app/git/cmd/git.exe`，临时前置 PATH 即可；克隆仓库 →
  以新版本文件整体覆盖 → 沿用仓库既有的中文 conventional 提交风格；
- **取证先于方案**：Motrix 的 `motrix.db`（任务/URI/错误）是现成的
  "用户实际发生了什么"记录，比复现快得多。

---

## 7. 如果重来一次

1. 先做"产物嗅探 + 直链解析"，再做下载器集成（顺序反了会白做一版浏览器方案）；
2. Motrix 集成直接上 RPC，跳过深度链接（除非要给别人做浏览器扩展式体验）；
3. 字体第一批就定免费可分发字体，避免"先雅黑后替换"的返工；
4. ctypes 第一天就立规矩：**所有 Win32 调用必须写全 argtypes/restype**。
